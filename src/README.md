# About MainSys.py
User can directly run mainsys.py after running enroll_user script and loading embeddings locally in there Dev Environment using local loading paths as required and can test system locally.
