# About main_system.py
User can directly run main_system.py after running enroll_user.py script and loading embeddings locally in there Dev Environment using local loading paths as required and can test system locally.
The Utils folder is provided just for higher level coding structure representational purpose.
